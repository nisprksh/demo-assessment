# kubectl create secret generic copilot-agent-env \
#   --from-literal=NODE_ENV=production \
#   --namespace=copilot-agent \
#   --dry-run=client -o yaml > k8s/secret.yml
#
# Or apply manually:
#   kubectl create secret generic copilot-agent-env \
#     --from-env-file=.env \
#     --namespace=copilot-agent
apiVersion: v1
kind: Secret
metadata:
  name: copilot-agent-env
  namespace: copilot-agent
type: Opaque
stringData:
  NODE_ENV: "production"
  PORT: "3000"
  # Add other secrets from your .env here
